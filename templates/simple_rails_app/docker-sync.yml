version: "2"
syncs:
  <appname>-sync:
    src: './'
    dest: '/app'
    sync_host_ip: 'auto'
    sync_excludes_type: 'Path'
    sync_excludes: ['.git','.gitignore','docker-compose.yml','docker-sync.yml','data/*']
    sync_strategy: unison
